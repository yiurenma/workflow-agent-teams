# 产品需求主文档 — Workflow 平台（`pm-doc-master.md`）

**文档版本：** 2.11  
**更新日期：** 2026-04-11  
**状态：** 草案 — 待人工确认  

**用途：** 单一产品基线。**文件名固定**为 `pm-doc-master.md`；仅 **文档版本** 与正文变更。本文 **全文为中文**；**不包含** HTTP 路径、方法、错误码、OpenAPI 等接口规格 —— 该类内容写在 **架构师文档** 中，与本产品说明书分离。

**域前缀：** **APP** = 应用管理 · **REC** = 运行记录与在线运行语义（产品侧可理解的行为）· **CV** = 画布（编排、试跑、说明）

**流程要求（见仓库根目录 `CLAUDE.md` → PM master baseline）：** 每处理一条 `workflow-agent-teams/TODO.md` 须更新本文件、递增 **文档版本**、在 **修订记录** 中登记。

---

## 1. 产品描述

**Workflow 平台** 支持对交易类消息进行 **可配置丰富化、条件判断与多通道下发**，在少写定制后端的前提下完成集成与运维。

三大功能线：

| 功能线 | 前缀 | 范围 |
|--------|------|------|
| 应用管理 | **APP** | 在 `/workflows` 的应用注册、设置、复制、配置历史与生命周期 |
| 运行记录管理 | **REC** | 执行记录的浏览、筛选、下钻；运行结果在记录中的含义（用户与对接方可理解） |
| 画布 | **CV** | `/workflows/$applicationName` 可视化编排、规则（JSONPath）、保存、试跑、Explain |

配置态与运行态职责分离、数据贯通。对接方如何调用、返回码表、网络协议等 **不在本文展开**。

---

## 2. 用户与角色

| 角色 | 侧重 |
|------|------|
| 集成工程师 | **APP** + **CV** |
| 应用开发者 | **REC**（提交请求与幂等、重试等 **业务语义**，非接口手册） |
| 运维 / QA / 分析 | **REC** + **CV**（试跑） |
| 产品 / 业务（偏只读） | **CV**（Explain） |

---

## 3. 用户故事与验收标准

### 【APP】3.1 应用管理

#### APP-US-01 — 浏览应用列表并进入目标应用

> **作为** 集成工程师，**我希望** 在桌面与移动浏览器上浏览清单并进入目标应用，**以便** 不因设备受限。

**桌面：** APP-AC-01-D1 名称、启用、描述、最后修改 · APP-AC-01-D2 分页（默认 5）+ 模糊搜索 · APP-AC-01-D3 表格在版心内纵向可滚 + 总数 · APP-AC-01-D4 空状态  

**移动：** APP-AC-01-M1 卡片同等核心信息、可滚动 · APP-AC-01-M2 等价查找/分页 · APP-AC-01-M3 桌面表/卡片切换，偏好持久化 · APP-AC-01-M4 空状态  

**通用：** APP-AC-01-G1 明确的「进入应用」入口（行/卡片主体以设计为准）· APP-AC-01-G2 不削弱 **CV** 平移/缩放/节点  

#### APP-US-02 — 创建应用

**桌面：** APP-AC-02-D1 创建入口、唯一名、进入 **CV** · APP-AC-02-D2 重名处理  

**移动：** APP-AC-02-M1 FAB 等、同流程 · APP-AC-02-M2 FAB 可拖、位置记忆、点按与拖动区分 · APP-AC-02-M3 与桌面规则一致  

**通用：** APP-AC-02-G1 不要求仅在单一终端才能完成  

#### APP-US-03 — 删除应用

**桌面：** APP-AC-03-D1 删除 + 确认  

**移动：** APP-AC-03-M1 ⋯ 删除，与桌面语义一致  

**通用：** APP-AC-03-G1 删除应用及其关联工作流定义 · APP-AC-03-G2 不因历史运行记录禁止删除 · APP-AC-03-G3 删除与「进 **CV**」入口不混同  

#### APP-US-18 — 编辑应用设置

**桌面：** APP-AC-18-D1 设置入口、界面列出的全部可编辑应用参数（与当前产品字段一致）· APP-AC-18-D2 保存反馈、行内校验  

**移动：** APP-AC-18-M1 字段覆盖面与桌面一致 · APP-AC-18-M2 规则一致  

**通用：** APP-AC-18-G1 列表即时反映 · APP-AC-18-G2 设置与进画布分离  

#### APP-US-10 — 跨应用复制工作流

**桌面：** APP-AC-10-D1 行/工具栏复制、预填源 · APP-AC-10-D2 弹窗流程  

**移动：** APP-AC-10-M1 ⋯ 复制、同流程 · APP-AC-10-M2 留在 **APP** 上下文，不自动进 **CV**（除非用户另行进入）  

**通用：** APP-AC-10-G1 复制须同时覆盖应用侧设置与完整工作流定义（含 **CV** 布局）；完成后目标应用状态与预期一致（技术实现见架构文档）  

#### APP-US-11 — 配置历史与回滚

**桌面：** APP-AC-11-D1 历史入口、分页列表/详情 · APP-AC-11-D2 仅回滚工作流定义、产生新修订记录  

**移动：** APP-AC-11-M1 ⋯ 历史，关键决策信息与桌面等价 · APP-AC-11-M2 不自动进 **CV**  

**通用：** APP-AC-11-G1 进入 **CV** 后与回滚结果一致  

---

### 【REC】3.2 运行记录管理

#### REC-US-19 — 浏览、筛选、下钻执行记录

**桌面：** REC-AC-19-D1 独立页/入口、列字段满足业务核对 · REC-AC-19-D2 筛选、分页、纵滚、总数 · REC-AC-19-D3 确认号、跟踪号、客户 ID 等在默认策略下可见  

**移动：** REC-AC-19-M1 同等筛选维度 · REC-AC-19-M2 列表/卡片可浏览、关键字段优先 · REC-AC-19-M3 详情与桌面承诺一致  

**通用：** REC-AC-19-G1 父子/重试链可理解 · REC-AC-19-G2 管理端能展示记录数据（数据来源与查询方式由架构/实现定义）；不削弱 **CV**  

#### REC-US-12 — 业务系统提交运行请求（产品语义）

*与终端形态无关。接口形态、报文格式、错误码 **见架构师文档**。*

**验收标准：** REC-AC-12-1 对接方可以按约定方式提交运行请求（负载格式、关联标识、业务确认信息等以 **发布给对接方的说明** 为准，不在本产品正文列 URL）。  

REC-AC-12-2 同步/异步行为与应用侧配置一致，且对调用方 **有书面说明、可验证**。  

REC-AC-12-3 重复提交同一业务键时系统 **不重复执行业务处理**；未知应用、重复键等场景下返回 **可分类、可沟通** 的失败说明（具体码表见架构文档）。  

#### REC-US-13 — 丰富化阶段（记录可解释）

*与终端无关。*

REC-AC-13-1 丰富化步骤在派发前按 **画布连线决定的顺序** 执行。  

REC-AC-13-2 每步规则为 JSONPath **全部匹配** 才执行；结果合并进运行时负载（字段含义见架构文档）。  

REC-AC-13-3 条件类步骤仅在规则匹配时应用其逻辑配置；不匹配则跳过，**无**隐式默认分支。  

REC-AC-13-4 丰富化结果在记录总状态上 **可被运维理解**。  

#### REC-US-14 — 下发阶段（记录可解释）

*与终端无关。*

REC-AC-14-1 下发在丰富化完成后执行。  

REC-AC-14-2 跟踪信息与各通道状态写入记录；最终总状态反映组合结果。  

REC-AC-14-3 通道模型可扩展未来类型。  

#### REC-US-15 — 幂等（产品语义）

*与终端无关。*

REC-AC-15-1 同一应用下同一业务关联键在系统中唯一。  

REC-AC-15-2 重复请求被识别并拒绝重复处理，且不产生重复业务后果。  

REC-AC-15-3 约定条件下的重试提交可按规则绕过重复校验（细节见架构文档）。  

#### REC-US-16 — 失败重试（产品语义）

*与终端无关。*

REC-AC-16-1 命中应用级重试策略时产生可关联的重试记录。  

REC-AC-16-2 重试次数可追溯；用尽后终态明确。  

---

### 【CV】3.3 画布

#### CV-US-04 — 打开并浏览画布

**桌面：** CV-AC-04-D1 有向图、步骤类型齐全、无单独 START 节点 · CV-AC-04-D2 平移缩放  

**移动：** CV-AC-04-M1 同等类型、触控平移缩放 · CV-AC-04-M2 可返回 **APP** 列表  

**通用：** CV-AC-04-G1 与 APP-US-01「进入应用」一致  

#### CV-US-05 — 添加步骤

**桌面：** CV-AC-05-D1 侧栏、拖拽创建 · **移动：** CV-AC-05-M1 类型集合等价、允许非拖拽 · **通用：** 新节点可选可配  

#### CV-US-06 — 连接步骤

**桌面：** CV-AC-06-D1 句柄、箭头、顺序与连线一致并可保存 · **移动：** CV-AC-06-M1 触控可连线 · **通用：** 持久化规则一致  

#### CV-US-07 — 配置步骤（含 JSONPath 规则）

**桌面：** CV-AC-07-D1 抽屉内分区清晰（描述、规则、动作/逻辑）· CV-AC-07-D2 再开显示已存值  

**移动：** CV-AC-07-M1 字段与桌面一致、滚动/焦点可用 · **通用：** CV-AC-07-G1 规则语义与 REC-US-13 一致  

#### CV-US-08 — 删除节点

**桌面：** CV-AC-08-D1 键盘/UI · **移动：** CV-AC-08-M1 不依赖物理键盘 · **通用：** CV-AC-08-G1 删关联边  

#### CV-US-09 — 保存工作流

**通用：** CV-AC-09-G1 保存须落库成功并有明确成功/失败反馈，失败不丢本地编辑 · CV-AC-09-G2 窄屏保存入口可达（保存通道归属管控面，规格见架构文档）  

#### CV-US-30 — 三区与 JSON 排版

**桌面：** CV-AC-30-D1 描述/规则/动作分区 + 约定 JSON 字段失焦排版 · **移动：** CV-AC-30-M1 结构保留、长 JSON 可用  

#### CV-US-31 — 多角色可读

**通用：** CV-AC-31-G1 副标题 · CV-AC-31-G2 可与实现字段对齐的提示（便于联调，非接口定义）  

#### CV-US-17 — 画布试跑

**桌面：** CV-AC-17-D1 弹窗、执行、响应摘要 · **移动：** CV-AC-17-M1 能力对等、可滚动关闭  

#### CV-US-20 — AI 说明（Explain）

**桌面：** CV-AC-20-D1 Explain 与 Run 并列、令牌引导、上下文完整、安全渲染 · **移动：** CV-AC-20-M1 信息完整度对等  

**通用：** CV-AC-20-G1 用于 AI 的令牌与说明请求 **不经过** 本平台自有业务后端（由浏览器直连或产品规定的安全路径完成）· CV-AC-20-G2 各已支持模型路径下提示信息完整度一致 · CV-AC-20-G3 设备授权等方式取得的访问凭据与手动粘贴路径 **同等可用**（在产品已声明支持的范围内）  

#### CV-US-21 — Explain 的第三方授权体验

**桌面：** CV-AC-21-D1 设备码类流程完整、成功后自动继续本次说明 · **移动：** CV-AC-21-M1 移动浏览器同流程 · **通用：** CV-AC-21-G1 在正式部署的前端环境可完整走通（验收以真实浏览器为准）  

#### CV-US-32 — 节点编辑器无障碍访问（屏幕阅读器）

> **作为** 使用屏幕阅读器编辑工作流节点的用户，**我希望** 节点编辑器抽屉在打开时能够宣告其用途，**以便** 我理解正在交互的区域并能高效导航表单。

**验收标准：** CV-AC-32-1 节点编辑器抽屉容器元素具有 `aria-label="Node Configuration"` · CV-AC-32-2 抽屉打开时，屏幕阅读器（NVDA、JAWS、VoiceOver）宣告"节点配置"或等效本地化文本 · CV-AC-32-3 修复适用于桌面（420px 宽抽屉）和移动（全宽抽屉）视口 · CV-AC-32-4 Axe DevTools 或类似无障碍扫描器报告抽屉上缺少 `aria-label` 的零违规

#### CV-US-33 — 节点编辑器标题文本符合 WCAG AA 对比度要求

> **作为** 低视力或色盲用户，**我希望** 节点编辑器标题文本与背景有足够的对比度，**以便** 我可以轻松阅读节点类型和配置标签而不费力。

**验收标准：** CV-AC-33-1 标题文本颜色从当前值更改为 `#6b6b75` 或更深，以实现至少 4.5:1 的对比度 · CV-AC-33-2 使用 WebAIM 对比度检查器或浏览器开发工具验证对比度，符合 WCAG 2.1 AA 级标准 · CV-AC-33-3 新颜色适用于节点编辑器标题中的所有文本（节点类型标签、章节标题）· CV-AC-33-4 桌面和移动视口均满足对比度要求 · CV-AC-33-5 Axe DevTools 或类似无障碍扫描器报告节点编辑器抽屉中的零颜色对比度违规

#### CV-US-34 — 节点编辑器表单字段描述文本符合 WCAG AA 对比度要求

> **作为** 视力受损或在强光环境下工作的用户，**我希望** 节点编辑器中的表单字段描述文本具有足够的颜色对比度，**以便** 我可以轻松阅读指导文本而不费力。

**验收标准：** CV-AC-34-1 表单字段描述文本颜色从 `text-zinc-400` (#a1a1aa) 更改为 `text-zinc-600` (#52525b)，以实现至少 4.5:1 的对比度 · CV-AC-34-2 修复应用于 HttpCallForm 和 LogicForm 组件中的以下 3 个描述段落："The name shown on the canvas..."（节点名称描述）、"Run only when..."（规则描述）、"What the system does..."（动作描述）· CV-AC-34-3 在白色 (#ffffff) 和浅灰色 (#fafafa) 背景上对比度均达到或超过 4.5:1 · CV-AC-34-4 视觉外观与整体设计系统保持一致（文本仍显示为辅助/帮助文本，而非主要内容）· CV-AC-34-5 受影响组件无其他样式更改

#### CV-US-35 — 界面视觉设计系统（Quiet Luxury）

> **作为** Workflow Studio 用户，**我希望** 界面采用精致、温暖的视觉设计，**以便** 平台传达品质感与专业性，而非冷淡的技术感。

**验收标准：** CV-AC-35-1 字体系统：标题采用衬线或精致无衬线字体（如 Crimson Pro、Lora、Instrument Serif），正文采用高质量 UI 字体；建立清晰的字体层级（页面标题、章节标题、正文、辅助文本）；所有文本符合 WCAG AA 对比度要求（正文 4.5:1，大文本 3:1）。

CV-AC-35-2 色彩系统：用温暖中性色替换冷灰色（zinc-400/500/600）；主文本为温暖深灰（#2d2d2d 范围），次要文本为温暖中灰（#6b6b6b 范围），边框为柔和温灰（#d4d4d4 范围）；引入大地色调强调色（主操作：陶土/粘土/柔金，成功：鼠尾草绿/橄榄绿，警告：琥珀/赭石，错误：柔和锈红/砖红，信息：板岩蓝/温灰蓝）；背景为温白/奶油色（#fafaf8 范围）与浅沙/米色（#f4f3f0 范围）；所有颜色组合符合 WCAG AA 对比度要求。

CV-AC-35-3 间距与层级：关键区域增加留白（导航头部 16-20px 垂直内边距，应用列表卡片 20-24px 内边距，表格行高 48-56px，表单字段间距 16-20px）；建立清晰视觉层级（页面级 32-40px，组件级 16-24px，元素级 8-12px）；响应式行为保持，间距在移动端适当缩放。

CV-AC-35-4 组件精致化：导航头部（精致字体、温暖背景、柔和阴影）；应用列表桌面表格（精致边框、适当行高、温暖悬停态）；应用卡片移动端（柔和阴影、圆角 8-12px、温暖背景、清晰层级）；按钮（主按钮温暖强调色、次按钮轮廓/幽灵样式、精致过渡）；状态标签（柔和颜色、圆角、适当内边距）；画布节点（精致样式、柔和阴影、温暖背景、清晰字体、微妙悬停/选中态）；抽屉与模态框（温暖背景、精致标题、适当内边距与间距、柔和阴影）。

CV-AC-35-5 动效与过渡：一致的过渡时长（快速交互 150-200ms，中速交互 250-300ms，慢速交互 300-400ms）；精致缓动函数（优先 ease-out 或自定义 cubic-bezier）；一致应用于按钮悬停、抽屉/模态框开关、下拉菜单、画布交互。

CV-AC-35-6 视觉验证：从 UAT 环境（https://workflow-ui-gamma.vercel.app/workflows）捕获基线截图（应用列表桌面/移动、画布视图、节点编辑器抽屉、模态框、记录列表）；从本地开发捕获对比截图（相同视图）；在 `STYLE_REFACTOR_LOG.md` 中记录视觉变更（字体、色彩、间距、组件精致化、前后截图引用）。

---

## 4. 非功能性需求（产品口径）

**APP / CV / REC** 界面类故事在双端核心路径一致；运行侧 **业务语义** 对对接方文档化；配置变更可审计；敏感数据按平台基线保护；**对外错误分类**清晰（细分码表归架构文档）；通道模型可扩展。

---

## 5. 范围外（当前）

统一 RBAC、批量执行、画布逐步回放、Explain 跨端同步凭据/服务端代管密钥/任意新增模型商（除非单独立项）、独立原生 App（本文以 Web 为基线）。

---

## 6. 决策记录（与 v1.2 对齐）

**REC** 记录纳入范围（**REC-US-19**）。**CV** 支持 FUNCTION_V2 / FUNCTION_V3，不支持旧版 FUNCTION v1。**APP** 应用参数全量可在界面编辑（**APP-US-18**）。重试按应用配置。通道模型可扩展。

---

## 7. 修订记录

| 文档版本 | 日期 | 说明 | 涉及 US/AC |
|----------|------|------|------------|
| 2.11 | 2026-04-11 | 界面视觉设计系统（Quiet Luxury）— 精致温暖的视觉重构 | CV-US-35, CV-AC-35-1~6 |
| 2.10 | 2026-04-11 | 节点编辑器表单字段描述文本颜色对比度修复（WCAG 2.1 AA 合规） | CV-US-34, CV-AC-34-1~5 |
| 2.9 | 2026-04-11 | 节点编辑器无障碍访问（WCAG 2.1 A+AA 合规） | CV-US-32, CV-US-33, CV-AC-32-1~4, CV-AC-33-1~5 |
| 2.8 | 2026-04-08 | 全文中文单正文；剔除 API/路径/错误码，改指架构文档；合并原 Part 1/2 | 全文结构与 REC/CV 表述 |
| 2.7 | 2026-04-08 | Part 1 / Part 2 命名 | 文首与标题 |
| 2.6 | 2026-04-08 | 主文档两段、切片单正文 | 文首 |
| 2.5 | 2026-04-08 | APP/REC/CV 基线双份正文 | 初版 |

---

*本文结束。每处理一条 `TODO.md` 须递增 **文档版本** 并更新 **修订记录**（见根目录 `CLAUDE.md` → PM master baseline）。`pm-doc-v*.md` 为切片，单正文、文风与 `CLAUDE.md` 一致；**接口与契约** 写在 `arch-doc-*.md`。*
