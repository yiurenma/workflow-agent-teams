# Agent teams to-do

Backlog for agent teams to pick up in order. Check items off as they ship.

## Open

- [ ] **Post-mortem & process: Explain — device-flow token `gho_` rejected as “Unrecognised token format” (test + review gap)** — **Reference basis (fix shipped):** Parent PR tracking submodule bump: [yiurenma/workflow#10](https://github.com/yiurenma/workflow/pull/10). **Code change (authoritative):** [workflow-ui@263b483](https://github.com/yiurenma/workflow-ui/commit/263b483c0b29671d947add279957b791644cedec) — `isValidToken` and `callAI` in `src/routes/workflows/-components/workflow-header/index.tsx` only allowed PAT-style GitHub prefixes (`ghp_`, `ghu_`, `ghs_`) while GitHub OAuth Device Flow returns **user OAuth access tokens** (`gho_…`), so users who completed device authorization still hit a false “Unrecognised token format” error. **Why this was not caught in testing or review (e.g. ASA, if used as a gate):** (1) **Happy-path coverage gap** — automated tests probably did not run a full **browser device-flow → poll token → Explain** sequence; manual QA may have used **“Paste token manually”** with a PAT (`ghp_`) only, which masked the bug. (2) **Duplicate logic / review blind spot** — validation existed in **two** places (`isValidToken` vs `callAI`); one list included `ghs_` but omitted `gho_`, easy to miss in review if reviewers checked only one site. (3) **No negative matrix** — no explicit test row “OAuth device-flow token must be accepted for GitHub Models path.” **Responsibility / process improvements (proposed):** **Test Manager** — add a versioned acceptance case: after device authorization, Explain must succeed (or fail only on upstream Models API), never on local prefix validation; include token-shape matrix (`gho_`, `ghp_`, `github_pat_`). **QA / interface testing** — execute device flow in **staging/production-like** build, not only manual PAT. **Frontend dev** — consolidate GitHub token-type detection in **one helper** shared by storage gate and `callAI` to prevent drift. **Code review checklist** — for auth features, diff must show **one** validation path or explicit tests proving both call sites match. **Output:** optional short note in `workflow-agent-teams/docs/` linking this TODO + PR above. **Recognition label for agents:** `TODO-postmortem-explain-github-gho-token-validation`.

- [ ] **Post-mortem & process: Explain + GitHub OAuth device flow (CORS / same-origin proxy PR)** — **Analysis to produce (PM + Test Manager + Delivery):** (1) **Root cause** — Device flow used `fetch` from the SPA origin to `https://github.com/...`, which browsers block via CORS; terminal `curl` succeeds, so the bug is environment-specific and easy to miss locally. (2) **Why it was not caught during development** — document gaps (e.g. no staged Vercel deploy before merge, tests assumed same behavior as curl, no explicit “browser cross-origin OAuth” checklist). (3) **Future prevention** — define **full test plan** for similar features: mandatory **real-browser** check on the **same host** as production (or staging with identical `vercel.json` / rewrites), Network tab confirms requests go to **same-origin proxy** paths when required, and negative test for direct third-party `fetch` where CORS applies. (4) **UAT / staging** — confirm whether the issue shipped because **real-environment** (Vercel) testing was missing; if yes, **add a standing rule**: during test **planning**, the Test Manager receives a **UAT or staging URL** (and branch/deployment notes) so acceptance runs match production infra, not only local `vite dev`. **Output:** short write-up in `workflow-agent-teams/docs/` (or appendix to test doc) + any checklist updates in `test-doc-*`. **Recognition label for agents:** `TODO-postmortem-explain-github-oauth-cors-uat`.

- [ ] **Artboard node editor — three-panel layout + smart text formatting** — Rework the per-node editor on the workflow artboard so it is unmistakably split into **three modules**: (1) **Node description** — what this node is for; (2) **Rules** — configuration for when/how the node applies; (3) **Action type** — what the node does (behavior / next steps). Each section should be visually and structurally distinct (clear headings, spacing, and scannable layout). In text fields inside the editor frame, add **basic format helpers**: if pasted or entered content is valid **JSON**, pretty-print / normalize it automatically; if it is not JSON, apply light structure improvements (line breaks, indentation, or similar) so unstructured input is easier to read — without corrupting user intent when parsing fails. **Recognition label for agents:** `TODO-artboard-node-editor-3panel-json-format`.

## Done

- [x] **Canvas AI / workflow explainer (Funlane-style)** — Added **Explain** button next to Run in `workflow-header/index.tsx`. On click: collects current canvas workflow data, calls AI API (Anthropic `sk-ant-…` or GitHub Models `ghp_…`/`ghu_…`), and displays a step-by-step explanation in a modal. Token stored in `localStorage` — no backend changes required.

- [x] **Explain: no GitHub token → send user to GitHub to authorize** — When Explain is clicked with no valid token, the UI now initiates GitHub OAuth Device Flow (`useGitHubDeviceFlow` hook). User sees a device code + "Open GitHub to authorize" button. On approval the token is stored and Explain runs immediately. "Paste a token manually" fallback preserved. Implemented in `workflow-ui` (`workflow-header/index.tsx` + `useGitHubDeviceFlow.ts`). No backend changes.
